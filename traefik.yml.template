api:
  dashboard: true
  insecure: false

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: cvat_cvat
  file:
    filename: /etc/traefik/dynamic.yml
